function bubblesort(arr){
//lets go with how bubble sort works,it works by selecting an element and then 
//and comparing if its greater than anyone and then putting it in the empty place
    let n=arr.length;    
    for (let i=0;i<n-1;i++){
        for (let f=0;f<n-1;f++){
            let 
        }
    }
}