function findsum(arr,k){
    let sum=0;
    let canst=(arr.length-k)+1;
    for (let i=0;i<=canst;i++){
        let temp;
        //use for loop here
    }
    return sum;
}
let arr=[2,3,1,2,4,3];
console.log(findsum(arr,4));
