//turn arr to ascenting and then checck if the other one is consecutive 
console.time("Runtime");
function longestconsecutivenumb(arr){
    let asc=[];
    for (let i=0;i<arr.length;i++){
        for (let f=0;f<arr.length;f++){
            let temp=
        }
    }    
}
console.timeEnd("Runtime");